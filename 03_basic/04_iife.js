
// IIFE - Immediately Invoked Function Expression

(function chai() {
    console.log(`DB CONNCETED`);
})();

( (name) => {
    console.log(`DB CONNCETED TWO ${name}`);
    })('mayank') // IIFE - Immediately Invoked Function Expression